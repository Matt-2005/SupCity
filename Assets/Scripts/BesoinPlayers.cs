using UnityEngine;

public class BesoinPlayers : MonoBehaviour
{
    public float soif = 50;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
