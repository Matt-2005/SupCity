using UnityEngine;

/// <summary>
/// Marque un objet comme étant effaçable (ex. : en mode suppression).
/// Peut être détecté par des systèmes comme le <c>BuildManager</c> pour être détruit.
/// </summary>
public class ObjectEraseable : MonoBehaviour
{
    /// <summary>
    /// Méthode appelée une fois au démarrage de l’objet.
    /// </summary>
    void Start()
    {
        
    }

    /// <summary>
    /// Méthode appelée à chaque frame.
    /// </summary>
    void Update()
    {
        
    }
}
